# WIP
will add a proper readme when finished

# what does it do?
runs locally and saves timestamps to a file trough a !clip command only accessable to mods and the broadcaster

after stream you run the clip generator and point it to your clips timestamp file and to the corresponding locally recorded vod and it will create clips at the timestamp with a set time